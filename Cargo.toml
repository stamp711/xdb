[package]
name = "xdb"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2"
nix = { version = "0.31", features = ["fs", "personality", "process", "ptrace", "signal"] }
rustyline = "17"

[build-dependencies]
cc = "1"
